/**
 * @param {String} orderId - Order id ref of order model
 */
const createPayment = async (orderId) => {
    // Logic for creating payment
};

/**
 * @param {String} paymentId - Payment id ref of payment model
 */
const retrievePayment = async (paymentId) => {
    // Logic for retrieving payment
};

/**
 * @param {String} paymentId - Payment id ref of payment model
 */
const updatePayment = async (paymentId) => {
    // Logic for updating payment
};

/**
 * @param {String} paymentId - Payment id ref of payment model
 */
const cancelPayment = async (paymentId) => {
    // Logic for canceling payment
};

module.exports = {
    createPayment,
    retrievePayment,
    updatePayment,
    cancelPayment
}